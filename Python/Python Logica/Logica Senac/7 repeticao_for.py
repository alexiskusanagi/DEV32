lista_nomes =["Sal", "Jo", "Brian", "Murphy"]
contador=1

print(f"listando os nomes de {len(lista_nomes)} pessoas")

for nomes in lista_nomes:
    print(f"{contador}° nome:{nomes}\n")
    contador=contador+1
